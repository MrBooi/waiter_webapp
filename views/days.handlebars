<div class="containers">
    <div class="">
        <h1>days </h1>
    <div id="">
        {{#each storedShifts}}
        <ul id="shiftdays">
            <h6>{{day}}</h6>
            {{#each Waiters}}
            <li class="{{../color}}">{{this}}</li>
            {{/each}}
        </ul>
        {{/each}}
    </div>
    </div>
  <a href="/clear">
  <button type="submit" id="clear">clear shifts</button>
  </a> 
</div>
 
